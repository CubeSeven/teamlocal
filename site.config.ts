// Deployment-specific site configuration.
// Update these values when deploying to a new environment or custom domain.

// For live GitHub Pages deployment (CubeSeven/teamlocal repo):
// - site must be the public Pages URL
// - base must be the repo name prefixed with a slash (Astro serves under /teamlocal/)
export const SITE = 'https://cubeseven.github.io/teamlocal';
export const BASE = '/teamlocal';
